@using FastBookCreator.Core
@using (Html.BeginForm("SetCulture", "Shared", FormMethod.Post, new { id = "CultureChangePostForm" }))
{
    <a  id="ul_langSelector">
        @Html.DropDownList("culture", HelperExtensions.LanguageSelectListItems(), new { @class = " show-tick", id = "langSelector", data_width = "110px" })
    </a>
}